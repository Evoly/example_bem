extends ../../layouts/default

block head
  - var pageTitle = 'Видео'

block content
  +inner()
    +e.IMG(src='assets/images/bg-4.png').bg
    +e.scroll-hider
      +e.scrollable
        +e.wrapper
          +header()
          +video().__section
  +footer()
  +popups()
